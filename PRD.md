# 지안찾기 PRD 및 작업지시서

## 1. 프로젝트 개요

- 프로젝트명: 지안찾기
- 상태: 기획 완료, 구현 전
- 목표 사용자: 중학교 2학년 딸과 가족
- 실행 환경: Windows PC와 모바일 브라우저에서 설치 가능한 PWA
- 게임 콘셉트: Windows 지뢰찾기의 규칙을 바탕으로, 지뢰 대신 지안이의 얼굴을 피해 안전한 칸을 모두 찾는 가족용 퍼즐 게임

### 해결할 문제

익숙한 지뢰찾기를 가족 사진으로 개인화해 짧게 반복해서 즐길 수 있고, 설치 후에는 인터넷 없이도 실행할 수 있는 가족용 게임을 만든다.

### 제품 원칙

- 원작의 핵심 규칙과 조작감은 익숙하게 유지하되 명칭과 시각 표현은 `지안찾기`만의 분위기로 바꾼다.
- 첫 실행에서 별도 설명 없이도 초급 게임을 시작할 수 있어야 한다.
- 얼굴 사진은 장식이 아니라 위험 칸을 공개했을 때 나타나는 핵심 반전 요소로 사용한다.
- 미성년자 얼굴 사진의 비공개와 최소 사용을 기능보다 우선한다.
- Windows 지뢰찾기의 명칭, 아이콘, 이미지, 효과음, 소스 코드를 복제하지 않고 게임 규칙만 참고한다.

### 주 레퍼런스

- 제품 레퍼런스: [무한 지뢰찾기 ZONE (App Store)](https://apps.apple.com/kr/app/%EB%AC%B4%ED%95%9C-%EC%A7%80%EB%A2%B0%EC%B0%BE%EA%B8%B0-zone/id6760695725)
- 참고할 핵심: 9×9 `Zone` 단위 진행, 추측 없이 논리로 해결하는 보드, 끝없이 이어지는 플레이 감각, 보드 집중형 미니멀 UI, 라이트·다크 테마, 광고·결제 없이 흐름을 끊지 않는 경험
- 지안찾기에 적용: MVP에서 9×9 한 Zone의 규칙·조작·개인화 재미를 먼저 검증하고, 이후 Zone을 이어 붙이는 무한 모드와 논리 풀이 보장 생성기를 추가한다.
- 그대로 복제하지 않을 것: 앱의 이름, 문구, 화면 배치, 색상, 아이콘, 지형, 벽 블록, 스크린샷, 애니메이션 및 기타 고유 자산
- 참고 기준일: 2026-08-01. 구현 시 레퍼런스가 바뀌더라도 이 PRD에 적힌 채택 요소를 기준으로 한다.

## 2. 용어와 기본 규칙

- `지안 칸`: 일반 지뢰찾기의 지뢰에 해당하는 숨겨진 칸. 열면 지안이 얼굴이 나타나고 해당 판이 끝난다.
- `안전 칸`: 지안 칸이 아닌 칸.
- `표시`: 열지 않은 칸에 깃발을 꽂거나 해제하는 동작.
- 숫자: 주변 8칸에 숨어 있는 지안 칸의 수.
- 승리: 모든 안전 칸을 열었을 때. 표시 개수나 위치만으로 승리 처리하지 않는다.
- 패배: 지안 칸을 열었을 때.

첫 클릭은 항상 안전해야 한다. 첫 클릭 위치에는 지안 칸을 배치하지 않으며, 가능하면 초급·중급에서는 주변 8칸도 안전하게 만들어 시작 시 빈 영역이 열리게 한다. 보드 배치는 첫 클릭 직후 생성한다.

## 3. 목표와 비목표

### MVP 목표

- 초급, 중급, 고급 난이도를 선택해 새 게임을 시작한다.
- 좌클릭/탭으로 칸을 열고, 우클릭 또는 모바일 표시 모드로 깃발을 설정한다.
- 빈 칸의 연쇄 열기, 숫자 표시, 승패 판정이 지뢰찾기 규칙대로 동작한다.
- 패배 시 모든 지안 칸에 동일한 가족 제공 얼굴 이미지를 보여준다.
- 타이머, 남은 지안 칸 추정 수, 새 게임 버튼을 제공한다.
- 앱 설치와 오프라인 재실행이 가능한 PWA로 동작한다.
- 브라우저에 난이도별 최고 기록만 로컬 저장한다.
- 9×9 보드를 하나의 `Zone`으로 느끼게 하는 간결한 보드 중심 화면을 제공한다.

### 비목표

- 로그인, 서버, 데이터베이스, 클라우드 동기화
- 사진 업로드, 카메라 촬영, 얼굴 인식, 이미지 생성 AI
- 광고, 결제, 온라인 랭킹, 멀티플레이
- 사용자의 이름·사진·플레이 기록을 외부로 전송하는 분석 도구
- Microsoft 지뢰찾기의 그래픽·사운드·상표 표현을 그대로 재현하는 것
- 앱스토어 또는 Microsoft Store 배포
- 무한 스크롤 보드와 모든 판의 논리 풀이 가능성 보장. 둘 다 MVP 이후 4단계 확장 범위다.

## 4. 난이도와 보드 규격

MVP 기본값은 아래와 같이 둔다.

| 난이도 | 보드 | 지안 칸 | 주요 환경 |
| --- | ---: | ---: | --- |
| 초급 | 9 × 9 | 10 | 모바일·입문 |
| 중급 | 16 × 16 | 40 | 태블릿·PC |
| 고급 | 30 × 16 | 99 | 데스크톱, 가로 스크롤 허용 |

- 초기 선택은 초급으로 한다.
- 보드와 지안 칸 수는 설정 객체에 모아 조정 가능하게 한다.
- 고급 보드는 칸을 지나치게 작게 축소하지 않고 보드 영역 안에서 가로 스크롤을 허용한다.
- 사용자 정의 난이도는 MVP 이후 실사용 피드백으로 결정한다.

## 5. 핵심 사용자 흐름

1. 사용자가 앱을 열고 `지안찾기` 제목, 난이도 선택, 새 게임 버튼을 본다.
2. 초급 보드가 준비되지만 지안 칸은 아직 배치되지 않는다.
3. 첫 칸을 열면 해당 칸이 안전하도록 보드를 생성하고 타이머를 시작한다.
4. 사용자는 숫자를 단서로 안전 칸을 열고 의심되는 칸에 표시한다.
5. 빈 칸을 열면 연결된 빈 칸과 경계 숫자 칸이 자동으로 열린다.
6. 지안 칸을 열면 얼굴 이미지가 나타나고 `지안을 찾았다!` 패배 결과를 보여준다.
7. 모든 안전 칸을 열면 `지안에게 들키지 않고 모두 찾았어요!` 승리 결과와 시간을 보여준다.
8. 최고 기록이면 기기에 저장하고 결과 화면에서 같은 난이도로 다시 시작할 수 있게 한다.
9. 지원 브라우저에서는 앱을 설치해 이후 오프라인에서도 같은 흐름으로 실행한다.

## 6. 기능 작업지시

### 6.1 게임 상태와 데이터

게임 상태는 최소 `ready`, `playing`, `won`, `lost`로 구분한다.

각 칸은 다음 정보를 가진다.

```js
{
  row,
  col,
  hasJian,
  adjacentCount,
  isOpen,
  isFlagged
}
```

- 보드 생성, 인접 수 계산, 칸 열기, 표시 전환, 승패 판정을 화면 렌더링과 분리한다.
- 첫 클릭 전에 타이머를 시작하거나 지안 칸을 배치하지 않는다.
- 열린 칸은 표시할 수 없고, 표시된 칸은 표시를 해제하기 전까지 열리지 않는다.
- 승리 또는 패배 후에는 보드 입력과 타이머가 멈춘다.
- 표시 카운터는 `전체 지안 칸 수 - 표시 수`로 보여주며 음수가 될 수 있다.

### 6.2 칸 열기와 연쇄 공개

- 숫자가 0인 안전 칸을 열면 인접한 안전 영역과 그 경계 숫자 칸을 한 번씩 연다.
- 큰 빈 영역에서도 호출 스택 오류가 나지 않도록 큐 기반 탐색을 권장한다.
- 열린 숫자 칸 주변의 표시 수가 숫자와 같을 때 주변 미개방 칸을 여는 `양쪽 클릭/빠른 열기` 기능은 2단계 개선으로 둔다.
- 패배 시 모든 지안 칸을 공개하고, 잘못 표시한 칸은 별도의 시각 기호로 구별한다.
- 승리 시 열리지 않은 지안 칸을 자동 표시하되 최고 기록 계산에는 영향을 주지 않는다.

### 6.3 입력 방식

- PC: 좌클릭으로 열기, 우클릭으로 표시 설정/해제, 보드 위 기본 컨텍스트 메뉴 차단.
- 모바일: 기본은 열기 모드이며 상단의 `열기 / 표시` 토글로 동작을 전환한다.
- 모바일 길게 누르기는 브라우저마다 오작동 가능성이 있고 레퍼런스 앱 사용자 리뷰에서도 의도치 않은 열기 문제가 반복 제기되었으므로 보조 수단으로만 검토한다. `열기 / 표시` 토글을 필수 입력 방식으로 유지한다.
- 키보드: 방향키로 칸 이동, `Enter` 또는 `Space`로 열기, `F`로 표시, 새 게임 버튼에 명확한 포커스 제공.
- 마우스, 터치, 키보드 입력이 동일한 게임 함수를 호출하게 구성한다.

### 6.4 화면 구성과 피드백

- 상단에 제목, 난이도, 남은 지안 칸 추정 수, 타이머, 최고 기록, 새 게임 버튼을 둔다.
- 보드는 닫힘, 열림, 표시, 숫자 1~8, 지안 칸, 잘못된 표시 상태를 색상뿐 아니라 모양과 텍스트로도 구별한다.
- 새 게임 버튼은 Windows 지뢰찾기의 얼굴 버튼을 복제하지 않고 `다시 숨기기` 또는 자체 캐릭터 표현을 사용한다.
- 패배 문구는 공포나 폭발보다 장난스럽고 긍정적인 분위기로 표현한다.
- 숫자는 충분한 대비를 확보하고, 얼굴 이미지 위에도 상태를 알아볼 수 있는 테두리 또는 배경을 둔다.
- 모든 조작 버튼은 모바일에서 최소 44 × 44 CSS px 터치 영역을 확보한다.
- `prefers-reduced-motion`에서는 승패 애니메이션을 줄인다.
- 보드를 중심에 두고 장식과 설명을 최소화하며, 라이트·다크 테마를 2단계에서 제공할 수 있도록 색상 토큰을 분리한다.

### 6.5 얼굴 이미지 자산

- 구현 시작 전 보호자가 사용 허락을 확인한 사진 1장만 별도로 준비한다.
- 원본 사진을 그대로 사용하지 않고 얼굴 중심의 정사각형 크롭본을 만든다.
- 게임 표시 크기에 맞게 축소·압축하고 사진의 위치 정보와 기타 메타데이터를 제거한다.
- 파일명에는 실명, 학교, 생년월일 등 식별 정보를 넣지 않는다. 코드에서는 중립적인 자산명으로 참조한다.
- 원본 사진은 프로젝트 폴더와 Git에 넣지 않는다.
- 크롭본도 공개 저장소에는 커밋하지 않는 것을 기본으로 한다. 공개 배포가 필요하면 사진 대신 비식별 캐릭터 이미지를 사용하는 별도 공개 빌드를 만든다.
- 사진이 없거나 로드에 실패해도 게임이 깨지지 않도록 기본 캐릭터 또는 이모지 대체 자산을 제공한다.

## 7. PWA 및 기술 경계

### 기본 기술안

- 서버 없는 정적 웹 앱으로 구현한다.
- HTML, CSS, JavaScript를 기본으로 하며 MVP에 꼭 필요한 경우에만 가벼운 빌드 도구를 채택한다.
- 외부 API, API 키, 인증, 유료 서비스는 사용하지 않는다.
- 최고 기록과 사용자 설정은 `localStorage`에만 보관한다.

### PWA 요구사항

- 웹 앱 매니페스트에 앱 이름, 짧은 이름, 시작 URL, `standalone` 표시 모드, 테마/배경색, 192px와 512px 아이콘을 정의한다.
- 서비스 워커가 앱 셸과 게임 자산을 캐시해 최초 정상 접속 이후 오프라인 실행을 지원한다.
- 앱 셸은 캐시 우선으로 제공하고, 버전이 바뀐 오래된 캐시는 활성화 단계에서 정리한다.
- 얼굴 이미지도 로컬 자산으로만 캐시하며 외부 URL에서 불러오지 않는다.
- 개발 중 서비스 워커 캐시가 변경 확인을 방해하지 않도록 버전 규칙과 초기화 방법을 README에 기록한다.
- 설치 기능은 HTTPS 또는 localhost 환경에서 검증한다.

### 배포 경계

- 1차 목표는 가족 기기에서의 로컬 실행과 설치 검증이다.
- 공개 URL 배포는 MVP 완료 조건이 아니다.
- 얼굴 사진이 포함된 빌드를 공개 호스팅하거나 공개 Git 저장소에 올리지 않는다.
- 가족 외 공유 또는 공개 배포가 필요하면 얼굴 사진 제거, 캐릭터 대체, 캐시·배포 산출물 재검사를 선행한다.

## 8. 단계별 구현 계획과 완료 기준

### 1단계: 순수 게임 로직

작업:

- 난이도 설정과 보드 생성
- 첫 클릭 안전 배치
- 인접 수 계산과 큐 기반 연쇄 열기
- 표시 전환, 승리·패배 판정
- 게임 로직 단위 테스트

완료 기준:

- 각 난이도에서 정확한 수의 지안 칸이 생성된다.
- 첫 클릭 칸은 100회 이상 반복 생성해도 안전하다.
- 모든 숫자가 실제 주변 지안 칸 수와 일치한다.
- 빈 영역 공개, 표시 보호, 승패 판정의 자동 테스트가 통과한다.

### 2단계: 플레이 가능한 반응형 UI

작업:

- 상태판, 난이도, 타이머, 새 게임 구성
- PC, 터치, 키보드 입력 연결
- 얼굴 대체 자산으로 승패 흐름 구현
- 최고 기록 로컬 저장

완료 기준:

- Windows PC에서 좌클릭과 우클릭으로 한 판을 끝낼 수 있다.
- 모바일에서 열기/표시 토글로 오입력 없이 한 판을 끝낼 수 있다.
- 초급·중급·고급 보드가 잘리거나 조작 불가능한 크기로 축소되지 않는다.
- 새 게임과 난이도 변경 시 이전 타이머와 보드 상태가 남지 않는다.

### 3단계: 얼굴 자산과 PWA

작업:

- 허락된 크롭본을 로컬 비공개 자산으로 연결
- 대체 자산과 오류 처리
- 매니페스트, 아이콘, 서비스 워커, 오프라인 캐시 구성
- 설치 및 오프라인 재실행 검증

완료 기준:

- 패배 시 얼굴 크롭본이 찌그러짐 없이 각 지안 칸에 표시된다.
- 사진이 없어도 대체 자산으로 모든 게임 기능이 동작한다.
- Windows의 지원 브라우저와 Android 계열 모바일에서 설치 가능 여부를 확인한다.
- 최초 온라인 로드 후 네트워크를 끊고 새로 실행해 게임이 동작한다.
- 저장소, 빌드 산출물, 캐시 대상에 원본 사진이나 식별 가능한 파일명이 없다.

### 4단계: 레퍼런스 기반 무한 Zone 확장

MVP 실사용 후 별도 승인으로 진행한다.

작업:

- 9×9 Zone을 상하좌우로 이어 붙이는 가상 보드와 화면 이동
- 현재 Zone 완료와 다음 Zone 개방 규칙
- 생성된 Zone만 메모리에 유지하고 먼 Zone을 정리·복원하는 상태 관리
- 생성 후 풀이 시뮬레이션으로 추측이 필요한 판을 걸러내는 `no-guess` 보드 생성
- 진행 Zone 수, 최고 연속 기록, 라이트·다크 테마
- 벽 블록과 지형 테마는 레퍼런스 고유 요소이므로 그대로 모방하지 않고 지안찾기만의 확장 규칙을 별도로 설계

완료 기준:

- 새 Zone을 이동·개방해도 기존 공개·표시 상태가 일관되게 유지된다.
- 동일한 시드에서 같은 보드가 재현된다.
- 자동 풀이기가 추측 없이 완료할 수 있다고 판정한 보드만 사용자에게 제공된다.
- 장시간 플레이 시 생성 Zone 수가 늘어도 메모리와 렌더링 성능이 정한 한도를 넘지 않는다.
- 레퍼런스 앱의 고유 화면과 자산을 복제하지 않고도 `끝없이 논리로 전진한다`는 경험이 전달된다.

## 9. 검증 계획

### 자동 검증

- 보드 크기와 지안 칸 개수
- 첫 클릭 안전 보장 및 제외 영역
- 가장자리·모서리·중앙 칸의 인접 수
- 연쇄 공개가 지안 칸과 표시 칸을 열지 않는지
- 모든 안전 칸을 열었을 때만 승리하는지
- 패배 후 입력 불가, 새 게임 후 완전 초기화
- 최고 기록이 승리 시에만 더 짧은 시간으로 갱신되는지

### 수동 검증

- Windows 최신 Chrome 또는 Edge에서 좌클릭, 우클릭, 키보드, 설치, 오프라인 실행 확인
- 모바일 세로 화면에서 스크롤과 열기/표시 토글 확인
- 첫 클릭, 빠른 연타, 표시 과다, 난이도 연속 변경, 마지막 안전 칸 상황 확인
- 사진 로드 성공·실패, 얼굴 비율, 대체 자산을 확인
- 화면 확대 200%, 키보드 포커스, 색상 외 상태 구분, 모션 감소 설정 확인
- `git diff`와 배포 산출물에서 원본 사진, 메타데이터, 개인식별정보가 없는지 별도 Reviewer가 확인

얼굴 사진과 PWA 캐시·배포 경계가 포함되므로 3단계 완료 전에는 별도 Reviewer가 실제 변경 파일, Git 추적 상태, 빌드 산출물, 서비스 워커 캐시 목록을 확인해야 한다.

## 10. 구현 시 하지 말 것

- PRD 승인 없이 서버, 로그인, 랭킹, 분석 도구를 추가하지 않는다.
- 얼굴 원본 또는 사진 메타데이터를 저장소, 문서, 테스트 픽스처에 넣지 않는다.
- 외부 이미지 호스팅, 얼굴 인식, 생성형 AI에 사진을 전송하지 않는다.
- Windows 지뢰찾기 자산이나 소스 코드를 가져오지 않는다.
- 게임 로직 검증 전에 시각 효과나 프레임워크 도입으로 범위를 넓히지 않는다.

## 11. 다음 작업

사용자가 이 프로젝트 폴더로 세션을 옮긴 뒤 1단계 게임 로직부터 구현한다. 첫 구현에서는 실제 얼굴 사진 대신 대체 자산을 사용하고, 9×9 초급 보드를 한 Zone으로 표현해 레퍼런스의 집중형 플레이 감각을 먼저 검증한다. 2단계까지 검증한 후 보호자가 사진 사용 범위와 공개 배포 여부를 다시 확인하고, 동의된 크롭본이 준비되었을 때만 3단계에 연결한다. 무한 Zone과 no-guess 생성기는 MVP 피드백 뒤 4단계 착수 여부를 별도로 결정한다.
